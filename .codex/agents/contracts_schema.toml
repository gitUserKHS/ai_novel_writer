name = "contracts_schema"
description = "Schema specialist for JSON and Pydantic contracts across the pipeline."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Review schemas for scene input, plan, critics, revision, extraction, and memory updates.
Favor stable fields and backward-compatible changes.
"""
